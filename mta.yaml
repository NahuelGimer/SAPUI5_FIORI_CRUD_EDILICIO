_schema-version: "3.2"
ID: ediliciofinal
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: ediliciofinal-destination-content
  type: com.sap.application.content
  requires:
  - name: ediliciofinal-destination-service
    parameters:
      content-target: true
  - name: ediliciofinal-repo-host
    parameters:
      service-key:
        name: ediliciofinal-repo-host-key
  - name: ediliciofinal-uaa
    parameters:
      service-key:
        name: ediliciofinal-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: ediliciofinal_html_repo_host
          ServiceInstanceName: ediliciofinal-html5-srv
          ServiceKeyName: ediliciofinal-repo-host-key
          sap.cloud.service: ediliciofinal
        - Authentication: OAuth2UserTokenExchange
          Name: ediliciofinal_uaa
          ServiceInstanceName: ediliciofinal-xsuaa-srv
          ServiceKeyName: ediliciofinal-uaa-key
          sap.cloud.service: ediliciofinal
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: ediliciofinal-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: ediliciofinal-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - ediliciofinal.zip
      name: ediliciofinal
      target-path: resources/
- name: ediliciofinal
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: ediliciofinal-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: ediliciofinal-destination-service
    service-plan: lite
- name: ediliciofinal-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: ediliciofinal-xsuaa-srv
    service-plan: application
- name: ediliciofinal-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: ediliciofinal-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
